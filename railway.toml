[build]
builder = "NIXPACKS"
watchPatterns = [
    "app/**",
    "requirements.txt",
    "railway.toml"
]

[deploy]
startCommand = "bash -c 'playwright install chromium && uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"