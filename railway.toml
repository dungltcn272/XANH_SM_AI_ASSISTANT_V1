[build]
builder = "NIXPACKS"
ignorePaths = ["README.md", "MINDSET.md", "SKILL.md", ".gitignore", ".dockerignore", "docs/**"]

[deploy]
startCommand = "streamlit run app/api/streamlit_ui.py --server.port $PORT --server.address 0.0.0.0"
restartPolicyType = "ON_FAILURE"
