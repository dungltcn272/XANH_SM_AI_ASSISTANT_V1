[build]
builder = "NIXPACKS"
ignorePaths = ["README.md", "**/README.md", "MINDSET.md", "SKILL.md", ".gitignore", ".dockerignore", "docs/**", "frontend/**"]

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
