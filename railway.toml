[build]
builder = "DOCKERFILE"

watchPatterns = [
  "backend/**",
  "app/**",
  "alembic/**",
  "alembic.ini",
  "requirements.txt",
  "Dockerfile",
  "railway.toml"
]

[deploy]
restartPolicyType = "ON_FAILURE"
